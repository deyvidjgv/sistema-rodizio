# MEMORY.md

## 2026-08-21 (5)
- **IDs del menú renombrados** (159 ítems en `shared/menu.js`): de `en1`/`pa3`/`ab2` (2 letras sin relleno) a `ENT-01`/`PAR-03`/`ANG-02` (3 letras mayúsculas por categoría + guión + 2 dígitos) — decisión del usuario para reconocer la categoría de un código a simple vista en reportes. Se hizo con un script de una sola pasada (no a mano) para evitar errores en 159 líneas; el mapeo completo viejo→nuevo quedó en el scratchpad de esta sesión, no en el repo. El campo `img` de cada ítem NO se tocó — sigue apuntando al `.webp` con el nombre viejo (`en1.webp`, etc.), documentado en `CLAUDE.md` para que no se asuma que `img` se puede derivar del `id`. Verificado que `id` se usa solo como key opaca (`ITEM_INDEX[id]`) en `mesero-app`/`panel-cliente`, sin regex de formato en ningún lado. Probado visualmente con Playwright (carrito de panel-cliente con los ids nuevos).

## 2026-08-21 (4)
- Reporte de caja "más completo": investigado con NotebookLM (notebook "Reportes y Exportación de Datos") qué le falta a un reporte de ventas de restaurante — mucho de lo enterprise (impuestos, descuentos/cortesías, costo de plato/ingeniería de menú, conciliación de caja) se dejó explícitamente para después, decisión del usuario, no implementar sin que lo pida.
- `panel-caja`: cobrar ahora pide método de pago — dos botones "Efectivo"/"Tarjeta" en vez de un solo "Confirmar pago" (`confirmarPago(id, metodoPago)`); se guarda `pedido.metodoPago`, y también `pedido.cajero`/`cajeroUsuario` (quién cobró, capturado del perfil logueado igual que `mesero`/`meseroUsuario`). El Excel exportado suma columnas Método de pago/Cajero y filas de subtotal por efectivo/tarjeta.
- `mesero-app`: `cancelarPedido()` ya NO hace `dbDelete` — marca `estado: "cancelado"` (+ `canceladoPor`, `tsCancelado`, `motivoCancelacion` opcional vía `prompt()`) para que el dueño tenga registro de cancelaciones. `mesaOcupada()`/`pedidoActivoDeMesa()` excluyen "cancelado" explícitamente; no aparece en `panel-cocina` ni `panel-caja` porque ninguno filtra por ese estado. No se construyó todavía una hoja/vista dedicada a ver cancelados — solo quedan en `/pedidos` para auditar a mano.

## 2026-08-21 (3)
- **Estado por ronda (cambio grande):** agregar una ronda nueva a un pedido que ya tenía una ronda `listo`/`entregado` ya NO resetea el ticket completo a `enviado` — cada ronda ahora avanza y se sirve por separado. Nuevo esquema: `pedido.rondaEstados` `{ronda: estado}` y `pedido.rondaPrepInicio` `{ronda: ts}` (cronómetro individual). Helpers nuevos en `shared/util.js`: `estadoDeRonda()`, `rondaEstadosCon()`, `estadoEfectivoTicket()` (calcula `pedido.estado` como la ronda pendiente menos avanzada, para que `panel-caja` siga funcionando sin cambios). Investigado antes de implementar con NotebookLM (notebook "Sistemas de Pedidos y Cocina (KDS)") citando el patrón de fulfillment por ítem de Toast/Oracle.
- `panel-cocina`: `marcar(id, estado)` → `marcarRonda(id, ronda, estado)`; cada grupo de ronda en el ticket tiene su propio botón/badge y cronómetro de preparación.
- `mesero-app`: `marcarServido(id)` → `marcarServido(id, ronda)` — el mesero puede recoger la ronda 1 sin esperar la ronda 2. Se endureció el guard de `editarPedido()`/`cancelarPedido()`/guardar-edición en `enviar()`: solo permitido si `rondaActual` sigue en 1 (si no, `p.estado==="enviado"` puede ser cierto solo por la ronda MÁS NUEVA, aunque una ronda vieja ya esté cocinándose).
- Verificado visualmente con harnesses de Playwright (no navegador real) para `panel-cocina` y la pestaña Pedidos de `mesero-app`, simulando el caso exacto reportado (ronda 1 lista + ronda 2 recién pedida).
- Feedback de diseño guardado en memoria global (`~/.claude/projects/.../memory/feedback_diseno_legibilidad.md`): priorizar legibilidad/función sobre estética "premium" en pantallas operativas.

## 2026-08-21 (2)
- `panel-cocina`: rediseño de ambiente — tema oscuro de alto contraste (override de tokens de `shared/theme.css` solo dentro de `panel-cocina/styles.css`, no afecta otras apps), tipografía más grande en tickets, hora exacta + relativa ("19:42 · hace 12 min"), y nueva sección Cliente/Mesero por ticket (cliente cae a mostrar el nombre del mesero si el pedido no vino de QR). Verificado visualmente con un harness en Playwright (screenshot), no en el navegador real.
- Nuevo campo opcional `cliente` en el esquema: `panel-cliente` deja escribir un nombre al armar el carrito → viaja en `/solicitudes/{mesaId}.cliente` → `mesero-app` lo traslada a `/pedidos/{id}.cliente` al confirmar la solicitud (no se toca en rondas posteriores). Documentado en `CLAUDE.md`.
- `panel-caja`: nuevo selector de mes (`#mesExportar`, por defecto el mes actual) que acota el Excel exportado; se agregó una segunda hoja "Resumen por plato" (código, descripción, categoría, cantidad vendida, total) con autofiltro, para buscar cuánto se vendió de un plato por código en el período.
- Pendiente/observado de paso (no corregido, fuera de alcance de esta sesión): el `onerror` de `<img class="plato-foto">` en `panel-cliente/app.js` hace `this.remove()` y luego lee `this.parentElement` — como `remove()` ya lo desprendió del DOM, `parentElement` da `null` ahí. Solo se manifestaría si una foto de plato realmente fallara en cargar (no ocurrió en pruebas con las fotos reales del repo).
- Preferencia del usuario: ir guardando avances en `MEMORY.md` periódicamente durante sesiones largas, no solo cuando se invoca `/checkpoint` explícitamente.

## 2026-08-21
- Skill `checkpoint` reparada: movida de `.claude/skills/checkpoint.md` (formato inválido + encoding roto) a `.claude/skills/checkpoint/SKILL.md` (UTF-8 correcto).
- Los cambios que estaban pendientes de commitear (`mesero-app/app.js`, `panel-cocina/app.js`, `shared/util.js`, `CLAUDE.md`, `panel-caja/app.js`) ya quedaron commiteados como `f7f15ac` ("Agrega sistema de rondas: pedir más en una mesa activa suma al pedido existente") — coincide con el sistema de rondas documentado en `CLAUDE.md`.
- Repo limpio al cierre de esta sesión: `git status` solo muestra `.claude/` sin trackear (infraestructura de skills/memoria, no código de la app); sin diff pendiente.
- Esta sesión de chat no generó cambios de código nuevos — solo se ejecutaron `/login` y este `/checkpoint`.
